---
- name: Collect required facts
  ansible.builtin.setup:
    gather_subset: ['!all','!min','distribution','architecture']
  tags: always

- ansible.builtin.import_tasks: install.yml
  tags: postgres.install

- ansible.builtin.import_tasks: dirs.yml
  tags: postgres.dirs
